const MESSAGE_TYPE = {
    WORKER_LOG: 'worker-log', // 日志
    WORKER_PROGRESS: 'worker-progress',// 进度
    WORKER_COMPLETED: 'worker-completed', // 完成
    WORKER_ERROR: 'worker-error' // 错误
}

module.exports = { MESSAGE_TYPE }

